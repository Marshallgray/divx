<%= simple_form_for(@talent) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :video %>
    <%= f.input :resume %>
    <%= f.input :bio %>
    <%= f.input :suburb %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
